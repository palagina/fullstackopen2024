export interface EntryType {
  id: number;
  date: string;
  visibility: string;
  weather: string;
  comment: string;
}

export interface NewEntryType {
  date: string;
  visibility: string;
  weather: string;
  comment: string;
}